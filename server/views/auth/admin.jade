extends ../layout
block content
  header
    h1 Admin

  .listings
    each listing in listings
      article.edit
        h2.title!= listing.info.name
        //- a(href="listings/edit/#{article._id}")